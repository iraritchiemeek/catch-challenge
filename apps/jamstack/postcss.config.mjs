// Tailwind CSS v4 processes styles through its PostCSS plugin. No autoprefixer or
// postcss-import needed — v4's plugin handles both.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
