import type { NextConfig } from "next";

// Minimal config. Challenge features (the paginated GitHub repo listing) are
// added later through the Build Loop.
const nextConfig: NextConfig = {};

export default nextConfig;
